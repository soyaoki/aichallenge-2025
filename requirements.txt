--extra-index-url https://download.pytorch.org/whl/cu121

# Dockerfile内のCUDA 12.2と互換性のあるCUDA 12.1向けPyTorchをインストール
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1


cvxpy
numpy==1.26.4   # 'numpy<2' の条件を満たしています
matplotlib
scipy
ecos
pandas
plotly
kaleido
rosbags
h5py
hdf5plugin
jaxtyping
opencv-python<=4.9
pyyaml
tqdm
hydra-core
omegaconf
scikit-learn
ultralytics
pyproj
pycocotools
grad-cam
tensorboard
tomli==2.3.0
packaging>=22.0

# openpilot_controller (comma.ai driving_supercombo) の推論ランタイム。
# CPU provider は常に使え、CUDA provider は cuDNN 9 がある場合のみ有効になる
# (`make openpilot-gpu-deps` を参照)。CUDA/cuDNN を pip 依存として要求しないため
# 上の PyTorch cu121 の pin とは衝突しない。
onnxruntime-gpu==1.23.2

# 強化学習用
gymnasium
stable-baselines3[extra]
